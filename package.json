{
  "name": "ad-player",
  "version": "0.1.0",
  "description": "Convenience wrapper for advertising video/audio content with Google IMA",
  "source": "src/index.ts",
  "main": "dist/ad-player.js",
  "module": "dist/ad-player.module.js",
  "unpkg": "dist/ad-player.umd.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "npm run -s build",
    "build": "microbundle",
    "start": "npm-run-all --parallel start:*",
    "start:serve": "serve",
    "start:bundle": "microbundle watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "glomex GmbH",
  "license": "Apache-2.0",
  "devDependencies": {
    "microbundle": "^0.12.0",
    "npm-run-all": "^4.1.5",
    "serve": "^11.3.0"
  },
  "dependencies": {
    "@alugha/ima": "^1.2.0"
  }
}
